import java.io.Writer;
import java.lang.*;
import java.util.*;

public class W$IfTest$000_txt extends wrime.output.WrimeWriter {
  private wrime.model.IfBean bean;

  public W$IfTest$000_txt(Writer writer) {
    super(writer);
  }

  protected void clear() {
    this.bean=null;
    super.clear();
  }

  protected void assignFields(Map<String, Object> model) {
    super.assignFields(model);
    this.bean=(wrime.model.IfBean)model.get("bean");
  }

  protected void renderContent() throws Exception {
    if ($$c.Equal(bean, null)) {
      if (bean.getBool()) {
      }
      if (bean.getBoolean()) {
      }
      if ($$c.NotEqual(bean.getReference(), null)) {
      }
    }
  }
}