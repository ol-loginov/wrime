import java.io.Writer;
import java.lang.*;
import java.util.*;

public class W$SimpleCompilerTest$006_txt extends wrime.WrimeWriter {
  private String hello;

  public W$SimpleCompilerTest$006_txt(Writer writer) {
    super(writer);
  }

  protected void clear() {
    this.hello=null;
    super.clear();
  }

  protected void assignFields(Map<String, Object> model) {
    super.assignFields(model);
    this.hello=(String)model.get("hello");
  }

  protected void renderContent() throws Exception {
  }
}